;; (use-package ps-ccrypt
;;   :load-path "~/.emacs.d/site-lisp/ccrypt"
;;   ;; :hoot (after-init-hook . awesome-tray-mode)
;;   )

;; (add-to-list 'load-path "~/.emacs.d/site-lisp/ccrypt")
;; (require 'ps-ccrypt)
;; (provide 'init-ccrypt)
